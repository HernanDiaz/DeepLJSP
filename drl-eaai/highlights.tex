%% Highlights del envio a EAAI: 3-5 puntos, maximo 85 caracteres cada
%% uno espacios incluidos (verificados por paper/verify_numbers.py).
\documentclass[12pt]{article}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\begin{document}
\section*{Highlights}
\begin{itemize}
\item A size-invariant deep RL policy schedules interval job shops of any size
\item Trained on one size class, it beats every priority rule on 70 instances
\item Thirty artifacts per paradigm meet GP rules at matched inference budgets
\item The inference budget decides which paradigm leads, significantly at all
\item Interval widths steer schedule selection, not the trained weights
\end{itemize}
\end{document}
